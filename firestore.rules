rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /scores/{document} {
      allow read: if true;  // Allow anyone to read scores
      allow write: if true;  // Allow authenticated users AND Facebook Instant Games users to write
    }
  }
}